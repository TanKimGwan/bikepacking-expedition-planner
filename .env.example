# Server-side provider credentials. Never prefix these with VITE_.
ORS_API_KEY=
GRAPHHOPPER_API_KEY=

# Provider selection; final values will be locked after validation.
ROUTING_PROVIDER=
GEOCODING_PROVIDER=
ELEVATION_PROVIDER=
SETTLEMENT_PROVIDER=overpass
